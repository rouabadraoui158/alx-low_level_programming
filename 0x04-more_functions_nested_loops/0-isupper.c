#include "main.h"
#include <stdio.h>

/**
 * _isupper( - check for uppercase character.
 * @c : is the character to check
 *
 * Return: 1 if is uppercase ,0 if not.
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
